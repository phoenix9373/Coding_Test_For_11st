const onClickBack = (currentApp) => {
  switch (currentApp) {
    case 'alarm':
      return () => {}
    case 'memo':
      return () => {}
    case 'photo':
      return () => {}
    default:
      return () => {}
  }
}

const onClickNew = (currentApp) => {
  switch (currentApp) {
    case 'alarm':
      return () => {}
    case 'memo':
      return () => {}
    case 'photo':
      return () => {}
    default:
      return () => {}
  }
}

export { onClickBack, onClickNew }
